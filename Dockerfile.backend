# 使用 OpenJDK 11 作为基础镜像
FROM openjdk:11-jdk-slim

# 设置工作目录
WORKDIR /app

# 将构建好的 jar 包复制到容器中
# 假设构建后的 jar 包名为 crm-system-1.0.0.jar
COPY target/crm-system-1.0.0.jar app.jar

# 暴露后端服务端口
EXPOSE 8080

# 启动 Spring Boot 应用
ENTRYPOINT ["java", "-jar", "app.jar"]
